
# vim: set ft=gpg:  using my  $vfv/after/syntax/gpg.vim

# $OSL/nodes/gpg-agent.conf  symlinked in
#  $MSn/set/6-WSL-nodes.sh
#  $OSAB/nodes-set/jo-0.sh

# Checks:
#  gpg-agent
#  ll ~/.gnupg/gpg-agent.conf
#  pb ~/.gnupg/gpg-agent.conf

allow-preset-passphrase
# - stackoverflow 49491679
pinentry-program /usr/bin/pinentry-curses

# 700 days (ie until system is reboot'd)
default-cache-ttl 60480000
# - timeout after last operation
max-cache-ttl 60480000
# - timeout after entering pw

